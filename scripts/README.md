# Benchmark runners

- `run-publication-study.sh` — Linux/macOS full COMMAG publication study
- `run-publication-study.ps1` — Windows PowerShell full COMMAG publication study

Both runners preserve the raw COMMAG cache and regenerate prepared data/results with the current code.
